*Setup Triggered* — *{{ symbol | esc }}* \({{ as_of | esc }}\)

{{ config | esc }} \| close {{ "%.2f" | format(close) | esc }} \| RSI {{ rsi | esc }} \| IVp {{ ivp | esc }} \| score {{ "%.2f" | format(score) | esc }}
