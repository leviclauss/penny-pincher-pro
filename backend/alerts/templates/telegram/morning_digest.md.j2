*Morning Digest* — {{ as_of | esc }}

*Macro* — VIX {{ "%.1f" | format(macro.vix) | esc }} \| term {{ "%.2f" | format(macro.term) | esc }} \| SPY {% if macro.spy_above_200ema %}above{% else %}below{% endif %} 200EMA

{% if screener_hits %}
*Screener hits* \({{ screener_hits | length | esc }}\)
{% for hit in screener_hits %}
• *{{ hit.symbol | esc }}* — {{ hit.config | esc }} \| close {{ "%.2f" | format(hit.close) | esc }} \| RSI {{ hit.rsi | esc }} \| IVp {{ hit.ivp | esc }} \| score {{ "%.2f" | format(hit.score) | esc }} \| earnings in {{ hit.next_earnings_days | esc }}d
{% endfor %}
{% else %}
_No screener hits today\._
{% endif %}

{% if earnings_today %}
*Earnings today*
{% for e in earnings_today %}
• {{ e.symbol | esc }} \({{ e.when | esc }}\)
{% endfor %}
{% endif %}
{% if positions_attention %}

*Positions needing attention*
{% for p in positions_attention %}
• {{ p.symbol | esc }} — {{ p.note | esc }}
{% endfor %}
{% endif %}
